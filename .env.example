# MongoDB Configuration
MONGO_CONNECTION_STRING=mongodb+srv://YOUR_USERNAME:YOUR_PASSWORD@YOUR_CLUSTER.mongodb.net/

# JWT Configuration
# NOTE: appsettings.json already has a default JWT key. Only set these environment
# variables if you want to override the defaults for production deployment.
JWT_KEY=default-dev-key-CHANGE-THIS-IN-PRODUCTION-min-32-characters-required
JWT_ISSUER=https://localhost:5000
JWT_AUDIENCE=https://localhost:5000
JWT_EXPIRY_MINUTES=480
